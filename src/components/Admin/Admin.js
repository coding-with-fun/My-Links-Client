import React from "react";
import Header from "./Header";

const Admin = () => {
    return (
        <div className="admin__container">
            <Header />
        </div>
    );
};

export default Admin;
